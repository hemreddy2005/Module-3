# Tuple in Python: Check Element Existence

## 🎯 Aim
To write a Python program that checks if the element `'n'` and the element `8` exist within a given tuple.

## 🧠 Algorithm
1. Define a tuple `x` with some letters and numbers.
2. Use the `in` operator to check if the string `'n'` exists within the tuple.
3. Use the `in` operator to check if the integer `8` exists within the tuple.
4. Print the results.

## 🧾 Program

```python
x = ('a', 'b', 'n', 5, 8, 10)

if 'n' in x:
    print("The element 'n' exists in the tuple.")
else:
    print("The element 'n' does not exist in the tuple.")

if 8 in x:
    print("The element 8 exists in the tuple.")
else:
    print("The element 8 does not exist in the tuple.")

```

## Output

![image](https://github.com/user-attachments/assets/5f69c4a6-6541-4851-81db-cc02d1872acd)

## Result
Hence the program is executed successfully.
